import React from 'react'
import ReactDOM from 'react-dom/client'
import App from '../chatbot-cs-ai.jsx' // Memanggil komponen Anda

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
